name: servepro_app
description: "ServePro — On-Demand Local Services Marketplace"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get: ^4.6.6
  google_fonts: ^6.1.0
  google_maps_flutter: ^2.5.3
  intl: ^0.19.0
  shimmer: ^3.0.0
  fl_chart: ^0.68.0
  pinput: ^5.0.0
  geolocator: ^11.0.0
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7
  permission_handler: ^11.3.0
  connectivity_plus: ^6.0.0
  url_launcher: ^6.2.5
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
